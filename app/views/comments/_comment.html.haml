.comment
  = show_image(comment.user, width:20)
  %b
    - if can? :show, comment.user
      = link_to comment.user_name, comment.user
    - else
      = comment.user_name
  %br
  %p= comment.body
  %p
    = link_to t('edit'), edit_idea_comment_path(current_idea, comment) if can? :update, comment
    = link_to t('delete'), idea_comment_path(current_idea, comment), method: :delete, data: { confirm: t('confirm')} if can? :delete, comment