<p><%= t('devise.greeting_informal', email: @resource.email) %></p>

<p><%= t('.lock_reason') %></p>

<p><%= t('.click_to_unlock') %></p>

<p><%= link_to t('.unlock_account'), unlock_url(@resource, unlock_token: @token) %></p>
