%h2 Listing ideas

- current_ideas.in_groups_of(3) do |group|
  .row
    - group.compact.each do |idea|
      .span4
        = show_idea_image(idea, width:100)
        %h4= link_to idea.name, idea
        = idea.description