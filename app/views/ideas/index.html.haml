%h2=t '.headline' #:index, scope: [:ideas]

- current_ideas.in_groups_of(3,false) do |group|
  .row
    = render group #.each do |idea|
    /.span4
    / = show_idea_image(idea, width:100)
    /%h4= link_to idea.name, idea
    /= idea.description